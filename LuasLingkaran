//library
#include <iosstream>
using namespace std;

//deklarasi variabel global
int phi, r;

//implementasi fungsi dan prosedur
void input(){
    cout << "Masukkan phi : ";
    cin >> phi;
    cout << "Masukkan r : ";
    cin >> r;
}
